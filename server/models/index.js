const UserModel = require('./usersModel');
const PostModel = require('./postModel');
const CommentModel = require('./commentModel');
const RatingModel = require('./raitingModel');

module.exports = {
    UserModel,
    PostModel,
    CommentModel,
    RatingModel,
};
